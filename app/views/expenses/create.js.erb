<%= render 'update', message: "The #{@expense.type.name} <strong>#{@expense.concept}</strong> for <strong>#{number_to_currency(@expense.amount)}</strong> on <strong>#{@expense.date}</strong> was created successfully!" %>
